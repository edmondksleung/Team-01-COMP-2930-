// AJAX to import data from firebase (detail of events)
$(document).ready(function(){
    $(".readMoreButton").click(function() {
        // Grabbing clicked id attribute
        // let idkey = $(this).attr("id");
        // console.log(idkey);
        alert("hi");
    });
})
